{
  "name": "wikiclaws-harness",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "WikiClaws posting/eval harness — the quality layer (pure Node, zero deps).",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "for f in scripts/*.mjs scripts/lib/*.mjs; do node --check \"$f\" || exit 1; done"
  }
}
